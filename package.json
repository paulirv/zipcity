{
  "name": "zip-city-lookup",
  "version": "1.0.0",
  "description": "Cloudflare Worker for zip-city lookup with bundled JSON data",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "wrangler": "^4.24.4"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "zipcode",
    "lookup",
    "api"
  ],
  "author": "",
  "license": "MIT"
}
